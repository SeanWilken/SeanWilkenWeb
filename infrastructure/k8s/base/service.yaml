apiVersion: v1
kind: Service
metadata:
  name: app
  namespace: store-prod
spec:
  selector: { app: app }
  ports:
    - name: http
      port: 80
      targetPort: 5000
  type: ClusterIP
